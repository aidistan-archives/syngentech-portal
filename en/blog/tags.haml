---
layout: default-en
title: Tag - SyngenTech Blog
---
.am-container
  {% for tag in site.tags %}
  %h2.am-margin-top-lg{id: "{{ tag[0] }}"} {{ tag[0] }} ({{ tag[1].size }})
  %ul
    {% for post in tag[1] %}
    %li
      {{ post.date | date: "%Y年%m月%d日" }}
      &raquo;
      %a{href: "{{ post.url }}"} {{ post.title }}
    {% endfor %}
  {% endfor %}
