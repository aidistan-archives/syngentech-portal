---
layout: default
title: 标签列表 - 合生博客
---
{% for tag in site.tags %}
.am-container
  %h1.am-margin-top-lg {{ tag[0] }}({{ tag[1].size }})
  %ol
    {% for post in tag[1] %}
    %li
      %span {{ post.date | date_to_string }}
      \&raquo;
      %a{href: "{{ post.url }}"} {{ post.title }}
      %a{href: "{{ post.url }}" }
        %img.am-img-responsive.am-margin-bottom-lg{src: "{{ post.image | prepend: site.url.assets }}" }
    {% endfor %}
{% endfor %}
